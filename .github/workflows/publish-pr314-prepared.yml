name: Validate PR 314 publication transport

on:
  push:
    branches:
      - ops/analyzer-readback-publish-314

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Actions admission
        run: echo "transport-admission-ok"
