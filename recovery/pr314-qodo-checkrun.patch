diff --git a/tests/test_analyzer_readback.py b/tests/test_analyzer_readback.py
index 9a501dc7a76c4e006c90cf4b49272b252f866fdd..25b5b03c1ba70364ec6a5b3ff887f3ace73681b1 100644
--- a/tests/test_analyzer_readback.py
+++ b/tests/test_analyzer_readback.py
@@ -1195,6 +1195,46 @@ class DeepSourceAnalyzerReadbackTests(unittest.TestCase):
         self.assertEqual("RUN_ASSOCIATION_AMBIGUOUS", document["coverage"]["reason"])
         self.assertNotIn("analysis_id", document)
 
+    def test_multiple_latest_check_run_ids_for_same_analyzer_are_ambiguous(
+        self,
+    ) -> None:
+        other_run = "11111111-1111-1111-1111-111111111111"
+        statuses = [
+            {
+                "context": "DeepSource: Python",
+                "source_kind": "check_run",
+                "app_slug": "deepsource-io",
+                "state": "success",
+                "target_url": (
+                    "https://app.deepsource.com/gh/ktogias/gnostoa/run/"
+                    f"{RUN_UID}/python/"
+                ),
+            },
+            {
+                "context": "DeepSource: Python",
+                "source_kind": "check_run",
+                "app_slug": "deepsource-io",
+                "state": "failure",
+                "target_url": (
+                    "https://app.deepsource.com/gh/ktogias/gnostoa/run/"
+                    f"{other_run}/python/"
+                ),
+            },
+        ]
+        document = analyzer_deepsource.diff_local_from_github(
+            repository="ktogias/gnostoa",
+            pull_number=312,
+            requested_head=HEAD,
+            observed_head=HEAD,
+            statuses=statuses,
+            comments=[],
+            observed_at=OBSERVED,
+        )
+        self.assertEqual("AMBIGUOUS", document["completeness"])
+        self.assertEqual("INCOMPLETE", document["coverage"]["status"])
+        self.assertEqual("RUN_ASSOCIATION_AMBIGUOUS", document["coverage"]["reason"])
+        self.assertNotIn("analysis_id", document)
+
     def test_multiple_latest_github_run_ids_across_analyzers_are_ambiguous(
         self,
     ) -> None:
diff --git a/tools/analyzer_deepsource.py b/tools/analyzer_deepsource.py
index 8b6a12fd5c0837c8bcd39e082e29cdf404b17a3f..d4135263c81a61771a5010ca06f0b552e9b9cb9c 100644
--- a/tools/analyzer_deepsource.py
+++ b/tools/analyzer_deepsource.py
@@ -806,7 +806,8 @@ def _github_status_summary(
     owner, name = normalized.split("/", 1)
     run_ids: set[str] = set()
     analyzer_states: dict[str, str] = {}
-    seen_sources: set[tuple[str, str]] = set()
+    seen_commit_status_analyzers: set[str] = set()
+    seen_check_runs: set[tuple[str, str]] = set()
     for status in statuses:
         if not _trusted_github_status(status):
             continue
@@ -828,11 +829,23 @@ def _github_status_summary(
         ):
             continue
         analyzer = match.group("analyzer")
-        source_key = (str(status.get("source_kind")), analyzer)
-        if source_key in seen_sources:
-            continue
-        seen_sources.add(source_key)
-        run_ids.add(match.group("run"))
+        run_id = match.group("run")
+        source_kind = str(status.get("source_kind"))
+        if source_kind == "commit_status":
+            # GitHub returns commit statuses newest-first, so only the first
+            # trusted status for one analyzer is current.
+            if analyzer in seen_commit_status_analyzers:
+                continue
+            seen_commit_status_analyzers.add(analyzer)
+        else:
+            # The check-run endpoint is queried in its latest mode, but more
+            # than one latest suite may still surface the same analyzer. Keep
+            # distinct run IDs so disagreement fails closed as AMBIGUOUS.
+            check_key = (analyzer, run_id)
+            if check_key in seen_check_runs:
+                continue
+            seen_check_runs.add(check_key)
+        run_ids.add(run_id)
         analyzer_states.setdefault(analyzer, str(state))
     return run_ids, analyzer_states
 
