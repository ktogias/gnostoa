# Change request

In the command help text, replace the heading `Destination` with
`Target destination`. This is a wording-only update: no runtime API, control
flow or data behavior should change. Update the existing snapshot expectation.
